# Contact form → Discord relay (used by api/contact.ts)
#
# Create a Discord webhook: Server Settings → Integrations → Webhooks →
# New Webhook → pick the channel → Copy Webhook URL. Paste it here for local
# `vercel dev`, and set the same value in the Vercel project's Environment
# Variables for production.
DISCORD_CONTACT_WEBHOOK_URL=
