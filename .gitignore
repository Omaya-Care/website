node_modules
.next
out
dist

# Large local artifact — exceeds GitHub 100MB limit
superpower-clone.tgz

# Playwright transient output (baselines live in visual/*-snapshots)
test-results
playwright-report

# Internal working notes (kept local, not tracked)
REACT_DOCTOR_FINDINGS.md

dist-server

# Local env / secrets — never commit the real webhook URL
.env
.env.*
!.env.example
.vercel

# Local scratch scripts (root-level _-prefixed one-offs: screenshot/measure helpers)
/_*.mjs
/_*.js
/_*.ts

# OS / editor junk
.DS_Store
Thumbs.db
.vscode
.idea
*.swp
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build / tooling caches
*.tsbuildinfo
.vite
.cache
